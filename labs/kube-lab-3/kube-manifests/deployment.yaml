apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-lab-3
  labels:
    app: kube-lab-3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kube-lab-3
  template:
    metadata:
      labels:
        app: kube-lab-3
    spec:
      containers:
        - name: web
          image: kube-lab-3
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP